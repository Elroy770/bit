const form=document.querySelector('#form'),msg=document.querySelector('#message');form.addEventListener('submit',async e=>{e.preventDefault();msg.textContent='שומר...';const r=await fetch('/api/transactions',{method:'POST',body:new FormData(form)});if(r.ok){form.reset();msg.className='ok';msg.textContent='העסקה נשמרה בהצלחה';}else{const x=await r.json().catch(()=>({}));msg.className='danger';msg.textContent=x.detail||'שמירה נכשלה';}});