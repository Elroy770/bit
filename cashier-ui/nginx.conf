server {
  listen 80;
  root /usr/share/nginx/html;
  location = /cashier/ { try_files /index.html =404; }
  location /cashier/ {
    rewrite ^/cashier/(.*)$ /$1 break;
    try_files $uri =404;
  }
}
